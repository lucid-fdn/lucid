/**
 * Workflow Credentials Components
 * 
 * Components for managing OAuth credentials in workflow nodes.
 * Integrates with Nango OAuth system.
 */

export { CredentialSelector, CredentialSelectorCompact } from './credential-selector'
export type { default as CredentialSelectorType } from './credential-selector'
